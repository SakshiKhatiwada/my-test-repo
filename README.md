commands:

git --version

git config --global user.name "Sakshi Khatiwada"

git config --global user.email "sakshikhatiwada37@gmail.com"

git init (.git folder created)

git commit -m "msg"
(snapshot is taken)

git remote add origin https:// 
(To connect Git and GitHub) 
(provided in github, one time setup)

git remote -v (to check if Git and GitHub are connected or not)

git branch -M main (move/rename the branchname forceful)

# log check garne

git log

git log --oneline

git checkout commit_hash/branch_name